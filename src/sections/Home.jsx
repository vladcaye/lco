import { About, Cornerstore, Contact } from '../sections'

export const Home = () => {
    return (
        <section className="home">
            <About />
            <Cornerstore />
            <Contact />
        </section>
    )
}